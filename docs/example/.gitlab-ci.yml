stages:
  - validate

validate:
  stage: validate
  image: alpine/helm:latest
  script:
    - helm repo add helm-apps https://flant.github.io/helm-apps
    - helm dependency update .helm
    - helm template myapp .helm --namespace myns --set "global.env=prod" > /dev/null
